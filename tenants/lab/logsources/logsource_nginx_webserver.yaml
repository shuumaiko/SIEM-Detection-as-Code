schema_version: 1.0

device_id: nginx-webserver

datasets:
  - dataset_id: nginx-webserver-access
    description: Nginx web server access logs
    category: security
    log_type: access
    enabled: true