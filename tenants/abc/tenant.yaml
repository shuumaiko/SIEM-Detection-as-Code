schema_version: 1.0

tenant_id: abc
name: ABC
environment: production
timezone: Asia/Bangkok

siem_id: splunk

metadata:
  display_name: ABC
  onboarding_source:
    - checkpoint-log.csv
    - mcafee-log.csv
  observed_time_offset: "+07:00"
  tenant_id_note: Normalized to lowercase `abc` to match tenant schema.
