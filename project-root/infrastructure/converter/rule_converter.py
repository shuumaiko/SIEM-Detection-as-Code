from domain.models.rule import Rule


class RuleConverter:
    """Convert source rule formats into canonical domain rule model."""

    def convert(self, rule: Rule) -> Rule:
        """Convert a single rule object (placeholder behavior)."""
        # Placeholder: convert raw rule syntax into canonical format.
        return rule
